.sortable {
    > * {
        will-change: transform, opacity
        transition: opacity .2s ease-in-out
    }
}

.ghost {
    opacity: 0
    transform: scale(0.9)
    transition-duration: 0s !important
}