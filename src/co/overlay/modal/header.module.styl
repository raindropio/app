.header {
    display: flex
    align-items: center
    min-height: 48px
    padding: 0 16px
}

.title {
    padding-right: 16px
    flex: 1
    font-weight: 500
    font-size: 16px
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    word-wrap: break-word;
}

.close {
    margin-right: -16px
}