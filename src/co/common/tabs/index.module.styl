@require "../../../css/buttons.styl"

.tabs {
	display: inline-flex
}

.item {
	@extend .button;
	position: relative;
	font-weight: 500

	&.active {
		padding-left: 4px;
		padding-right: 4px;
		
		b {
			font-weight: 500
			border-radius: 20px;
			padding: 2px 10px;
		}
	}
}