.items {
    display: grid
    grid-auto-flow: column
    grid-auto-columns: minmax(5rem, 7.5rem) //minmax(200, 300)
    padding-bottom: var(--padding-medium)
    overflow: auto
    scroll-snap-type: x mandatory
}