@include "list.styl"

simpleCoverSize = 16px

.view-simple .element {
	&:after {
		left: rem(simpleCoverSize + 32px)
	}
	
	.cover {
		width: rem(simpleCoverSize)
		height: rem(simpleCoverSize)
	}
	
	.cover, .cover img {
		display:block;
	}

	.cover {
		background: transparent;
		
		&.cover-placeholder {
			
			
            &:after {
            	display:none;
                background-size: 11px 7px !important;
            }
        }
	}
}